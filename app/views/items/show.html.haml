%body
  .wrapper
    = render "top-contents"
.main
  .content
    .content-box
      .image
        .image__name
          =@item.name
        .image__top
          = image_tag @item.images[0].image.url, size:"500x300" 
         
        .image__bottom
          - @images[1..3].each do |image|
            =image_tag("#{image.image}",class:'image__bottom__all' , size: '200x200')
      .price
        .price__integer
          =@item.price
        .price__index
          (税込み) 送料込 プライスレスです
      .text
        .text-form
          =@item.explain

      .index
        %table
          %tr
            %th 出品者
            %td
              =@item.user.nickname
          %tr
            %th カテゴリー
            %td 
              =link_to"#{@item.category.root.name}","#"
              %br
              =link_to"#{@item.category.parent.name}","#"
              %br
              =link_to"#{@item.category.name}","#"

          %tr
            %th ブランド
            %td 
              =@item.brand   
          %tr
            %th 商品の状態
            %td 
              =@item.status.name
          %tr
            %th 配送料の負担
            %td 
              =@item.delivery_cost.name
          %tr
            %th 発送元の地域
            %td 
              =link_to "#{@item.area.name}","#"
          %tr
            %th 発送日の目安
            %td 
              =@item.limit.name
      .buy
        = link_to item_purchase_index_path(@item) do
          .buy-btn
            購入する！
      .delete
        - if user_signed_in? && current_user.id == @item.user_id
          = link_to item_path(@item.id), method: :delete do
            .delete-btn
              削除する！！
      .option
        %ul.option-btns
          %li.option-like
            = icon('fa','star',class:"option__btn")
            .favorite
              お気に入り
        %ul
          %li.option-caution
            = icon('fa','flag',class:"option__btn")
            .caution
              不適切な商品を通報
    .comment
      = form_with html: {class: "form"}, local: true do |f|
        .comment-form
          = f.text_area :content, class: "comment-form__text", placeholder: 'コメント下さい何でもします'
          .comment-form__attention
            相手のことを考え丁寧なコメントを心がけましょう。
            不快な言葉遣いなどは利用制限や退会処分となることがあります。
          .comment-form__btn
            = f.submit "コメントする！", class: "comment-form__send"
            = icon('fa','comment',class:"comment-form__icon")
    %ul.next-link
      %li.next-link__1
        = link_to "#" ,class: "btn" do
          < 前の商品
      %li.next-link__2
        = link_to "#" ,class: "btn" do
          次の商品 >
    .related-items
      .related-items-category
        同カテゴリーをもっと見る
      .product__lists
        .product__list
          = link_to '#' do
            = image_tag 'pict-reason-02.jpg', class:'product__list__image'
            .product__list__body
              .product__list__body__name
                100億ドルの夜景
              .product__list__body__details
                .product__list__body__details__price
                  100億円
                .product__list__body__details__like
                  ★ 0
              .product__list__body__tax
                (税込)
.wrapper
  = render "bottom-contents"